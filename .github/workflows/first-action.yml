name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
      runs-on: ubuntu-latest
      steps:
        - name: Print greeting
          run: echo "hello there"

        - name: Print fairwell
          run: |
                echo "bye there"
                echo "see you later"
